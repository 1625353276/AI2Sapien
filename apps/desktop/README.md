# @ai2sapien/desktop

Electron + React desktop shell. This package is intentionally a placeholder until Spike 0 proves the complete Codex App Server login, rate-limit, streaming, cancellation, resume, and approval flow.

The renderer will only access Learning Core through a typed preload bridge.
